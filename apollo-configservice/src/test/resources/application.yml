spring:
  application:
    name: apollo-configservice

server:
  port: ${port:8080}
  
logging:
  level:
    org.springframework.cloud: 'DEBUG'
  file: ${home}/temp/opt/logs/${ctrip.appid}/apollo-configservice.log

ctrip:
  appid: 100003171